import type { HouseCategory } from '../types/houses';

// Функция для получения домов по категории
export async function getHousesByCategory(category: HouseCategory) {
  // Здесь можно добавить логику получения домов из API или локальных данных
  // Для примера возвращаем пустой массив
  return [];
}
